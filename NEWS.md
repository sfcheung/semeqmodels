# semeqmodels 0.1.0

- First CRAN release.

# semeqmodels 0.0.1

- First public release for testing.
  (0.0.1)

- Added tests using `dagitty`.
  (0.0.1.1)

- Updated `eq_models_internal()`
  to automatically mark the original
  models, and updated `partables_plots()`
  to retrieve the original model if
  marked. (0.0.1.2)

- Updated `eq_df_models()` and
  `add_k()` to handle errors from
  `modelbpp::gen_models()`.
  (0.0.1.3)

- Made the progress bars of
  `eq_df_models()` more informative.
  (0.0.1.4, 0.1.0)