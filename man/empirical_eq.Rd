% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emp_eq.R
\name{empirical_eq}
\alias{empirical_eq}
\title{Empirical Equivalent Models}
\usage{
empirical_eq(
  ptables,
  original_model,
  ...,
  se = "none",
  parallel = TRUE,
  ncores = max(parallel::detectCores(logical = FALSE) - 1, 1),
  make_cluster_args = list(),
  progress = TRUE,
  tolerance = 1e-05
)
}
\arguments{
\item{ptables}{A list of the class
\code{partables}.}

\item{original_model}{The original
model, fitted by \code{\link[lavaan:lavaan]{lavaan::lavaan()}}
or its wrapper, such as \code{\link[lavaan:sem]{lavaan::sem()}}.}

\item{...}{Optional arguments to be
used when fitting models to the data.
Usually can be omitted.}

\item{se}{How standard errors are to
be computed. To be passed to \code{lavaan}.
The default, \code{"none"}, is sufficient
because the standard errors are not
needed to check whether two models
are empirically equivalent.}

\item{parallel}{Whether parallel
processing will be used when fitting
models. Default is \code{TRUE}.
To be passed
to \code{\link[modelbpp:fit_many]{modelbpp::fit_many()}}.}

\item{ncores}{The number of CPU cores
to use when parallel processing is used.
To be passed
to \code{\link[modelbpp:fit_many]{modelbpp::fit_many()}}.}

\item{make_cluster_args}{Additional
arguments to be passed to
\code{\link[modelbpp:fit_many]{modelbpp::fit_many()}} when creating
a cluster for parallel processing.
To be passed
to \code{\link[modelbpp:fit_many]{modelbpp::fit_many()}}.}

\item{progress}{Whether the testing
progresss will be displayed
on screen.}

\item{tolerance}{The maximum difference
in model chi-squares for two models
to be considered empirically equivalent.}
}
\value{
The function \code{\link[=empirical_eq]{empirical_eq()}}
returns a list of the class \code{partables},
of models that are empirically
equivalent to the original model.
}
\description{
Identify model(s) in
a list of parameter tables empirically
equivalent to the original model.
}
\details{
The function \code{\link[=empirical_eq]{empirical_eq()}}
checks the model degrees of freedom
and model chi-squares of a list of
models against an original model,
fitted to a sample, to identify models
that are \emph{empirically} \emph{equivalent}
to the original model \emph{in this sample}.

Two models are empirically equivalent
if they (a) have the same model degrees
of freedom and (b) have a difference
in model chi-squares equal to or less
than a tolerance (controlled by the
argument \code{tolerance}).

If two models are mathematically
equivalent, then they must be empirically
equivalent.

However, even if two models are not
mathematically equivalent, they may
still be empirically equivalent for
a sample.
}
\examples{

library(lavaan)

# Model 1

mod1 <-
"
fx =~ x1 + x2 + x3
fm =~ m1 + m2 + m3
fy =~ y1 + y2 + y3
fm ~ fx
fy ~ fm + fx
"
fit1 <- sem(
          model = mod1,
          data = data_test_3_factor_3_item
        )
fit1_1_more <- drop_k(fit1)
fit1_1_more_1_less <- lapply(
  fit1_1_more,
  add_k
)

# Model 3

mod3 <-
"
fx =~ x1 + x2 + x3
fm =~ m1 + m2 + m3
fy =~ y1 + y2 + y3
fm ~ fx
fy ~ fm
"
fit3 <- sem(
          model = mod3,
          data = data_test_3_factor_3_item
        )
fit3_1_more <- drop_k(fit3)
fit3_1_more_1_less <- lapply(
  fit3_1_more,
  add_k
)

# All equivalent
ptables1 <- combine_ptables(fit1_1_more_1_less)

# Some equivalent
ptables3 <- combine_ptables(fit3_1_more_1_less)

eq_out_1 <- empirical_eq(
          ptables1,
          original_model = fit1,
          parallel = FALSE
        )

eq_out_3 <- empirical_eq(
          ptables3,
          original_model = fit3,
          parallel = FALSE
        )

}
\references{
Pesigan, I. J. A., Cheung, S. F.,
Wu, H., Chang, F., & Leung, S. O. (2026).
How plausible is my model? Assessing
model plausibility of structural
equation models using Bayesian
posterior probabilities (BPP).
\emph{Behavior Research Methods}, \emph{58}(3),
73.
\doi{10.3758/s13428-025-02921-x}
}
\seealso{
\code{\link[modelbpp:fit_many]{modelbpp::fit_many()}} for
the function use to fit the models.
}
