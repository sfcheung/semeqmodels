% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eq_partables_helpers_lav.R
\name{eq_partables_helpers}
\alias{eq_partables_helpers}
\alias{eq_lavInspect}
\alias{eq_fitMeasures}
\alias{eq_df}
\alias{eq_chisq}
\alias{eq_fits}
\alias{[.eq_partables}
\alias{[<-.eq_partables}
\alias{[[<-.eq_partables}
\title{Helpers for 'eq_partables' Object}
\usage{
eq_lavInspect(object, ..., simplify = FALSE)

eq_fitMeasures(object, ..., output_format = c("data.frame", "list"))

eq_df(object)

eq_chisq(object)

eq_fits(object)

\method{[}{eq_partables}(x, i)

\method{[}{eq_partables}(x, i) <- value

\method{[[}{eq_partables}(x, i) <- value
}
\arguments{
\item{object}{An \code{eq_partables} object.}

\item{...}{Optional arguments to be
passed to the \code{lavaan} functions
to be called. For the subsetting and
assignment methods, they are arguments
to be passed to those methods.}

\item{simplify}{To be passed to
\code{\link[=sapply]{sapply()}}. Note that the default is
\code{FALSE}, different from \code{\link[=sapply]{sapply()}}.}

\item{output_format}{The format of
the output of \code{\link[=eq_fitMeasures]{eq_fitMeasures()}}.
If \code{output_format} is \verb{"data.frame}, then
the output will be a data frame with
the number of columns equal to the
number of models, and rows equal to
the number of values returned by
\code{\link[lavaan:fitMeasures]{lavaan::fitMeasures()}}.
If \code{output_format} is \code{"list"}, then
the output will be a list of numeric
vectors.}

\item{x}{A 'eq_partables'-class object.}

\item{i}{A numeric vector of model position(s),
a character vector of model name(s),
or a logical vector of model(s) to be selected.}

\item{value}{The value(s) to be
assigned to the \code{eq_partables} object.}
}
\value{
The function \code{\link[=eq_lavInspect]{eq_lavInspect()}}
returns the output of
\code{\link[lavaan:lavInspect]{lavaan::lavInspect()}}. Whether it
is a vector, list, or other type of
objects depends on the argument
\code{simplify}, used by \code{\link[=sapply]{sapply()}}.

The function \code{\link[=eq_fitMeasures]{eq_fitMeasures()}}
returns the output of
\code{\link[lavaan:fitMeasures]{lavaan::fitMeasures()}}. The format
is determined by \code{output_format}.

The function \code{\link[=eq_fits]{eq_fits()}} returns a
list of \code{lavaan} outputs for the
models in \code{object}. If absent for
a model, the value returned is \code{NULL}.

The \code{[}, \verb{[<-}, and \verb{[[<-} methods return
an \code{eq_partables} object.
}
\description{
Helpers to work
with an
\code{eq_partables} object.
}
\details{
Although the functions are designed
to work with the output of \code{\link[=eq_models]{eq_models()}},
they also work for a list of models
(parameter tables), except for the
methods specifically for an \code{eq_partables}
object.

The function \code{\link[=eq_lavInspect]{eq_lavInspect()}}
calls \code{\link[lavaan:lavInspect]{lavaan::lavInspect()}} on the
elements of an \code{eq_partables} object.

The function \code{\link[=eq_fitMeasures]{eq_fitMeasures()}}
call \code{\link[lavaan:fitMeasures]{lavaan::fitMeasures()}} on the
elements of an \code{eq_partables} object.

The function \code{\link[=eq_df]{eq_df()}} is a wrapper
that call \code{\link[=eq_fitMeasures]{eq_fitMeasures()}} with
\code{fit.measures} set to \code{"df"}. It
always return a numeric vector.

The function \code{\link[=eq_chisq]{eq_chisq()}} is a wrapper
that call \code{\link[=eq_fitMeasures]{eq_fitMeasures()}} with
\code{fit.measures} set to \code{"chisq"}. It
always return a numeric vector.

The function \code{\link[=eq_fits]{eq_fits()}} extracts
the \code{lavaan} outputs stored for each
model, if present.

The class \code{eq_partables} has
\code{[}, \verb{[<-}, and \verb{[[<-} methods for extracting
and changing elements.

Though available, it is not advised
to assign models to an \code{eq_partables}
object because there is no guarantee
that the names still reflect how the
models are created.
}
\examples{

library(lavaan)

# Model 1

mod1 <-
"
fm ~ fx
fy ~ fm
"
fit1 <- sem(
          model = mod1,
          data = data_test_3obvs,
          fixed.x = FALSE
        )

# Remove 'parallel = FALSE' or set parallel to TRUE
# for faster generation.
out <- eq_models(
  original_model = fit1,
  parallel = FALSE
)
out

eq_lavInspect(out, "implied")


eq_fitMeasures(out, c("cfi", "tli"))



eq_df(out)


eq_fits(out)


out1 <- out[2:3]

out1

}
