% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_eq_models.R
\name{eq_df_models}
\alias{eq_df_models}
\title{Generate Models with the Same Degrees of Freedom}
\usage{
eq_df_models(
  sem_out,
  fit_models = FALSE,
  loadings_to_exclude_from_drop = "all",
  must_not_drop = NULL,
  must_not_add = NULL,
  se = "none",
  exclude_x_y_ecov = TRUE,
  parallel = TRUE,
  ncores = max(parallel::detectCores(logical = FALSE) - 1, 1),
  progress = interactive(),
  gen_models_progress = FALSE,
  short_names = TRUE,
  must_not_add_nil_parameters = TRUE
)
}
\arguments{
\item{sem_out}{A \code{lavaan} object, which
is usually the output of \code{\link[lavaan:sem]{lavaan::sem()}}
or similar wrappers. Models will be
generated from this model.}

\item{fit_models}{Whether the models
will be fitted to the data. To be
passed to \code{\link[=drop_k]{drop_k()}} and \code{\link[=add_k]{add_k()}}.
Usually should be left as \code{FALSE}, the
default, to speed up the search because
usually only the final set of models
need to be fitted.}

\item{loadings_to_exclude_from_drop}{How factor loadings will be handled.
Default is \code{"all"} and no factor loadings
will be dropped.
To be passed to \code{\link[modelbpp:gen_models]{modelbpp::gen_models()}}.
This argument should not be changed.
Included for internal use.}

\item{must_not_drop}{A character vector
of parameters that must not be removed,
and so will not be modified. To be
passed to \code{\link[modelbpp:gen_models]{modelbpp::gen_models()}}.}

\item{must_not_add}{A character
vector of parameters that must not
be added. To be passed to \code{\link[modelbpp:gen_models]{modelbpp::gen_models()}}.}

\item{se}{Whether standard error will
be computed. This argument will be
passed to \code{\link[lavaan:lavaan]{lavaan::lavaan()}}.
Default is \code{"none"}, and
this setting overrides the setting
in \code{object}. The standard errors are
irrelevant in checking whether two
models are equivalent.}

\item{exclude_x_y_ecov}{If \code{TRUE},
models with a covariance
between a variable (latent or observed)
and the
error term of another variable it
predicts, either directly or indirectly,
will be excluded. The screening is
implemented by \code{\link[=remove_x_y_ecov]{remove_x_y_ecov()}}.}

\item{parallel}{Whether parallel
processing will be used. If possible,
should be set to \code{TRUE} to speed up
the search.}

\item{ncores}{The number of CPU cores
to be used if \code{parallel} is \code{TRUE}.}

\item{progress}{If \code{TRUE}, messages
will be displayed to report the
progress of the search.}

\item{gen_models_progress}{If \code{TRUE},
then progress in each call to
\code{\link[=drop_k]{drop_k()}} or \code{\link[=add_k]{add_k()}} will also
be displayed.}

\item{short_names}{If \code{TRUE}, then
short names (from the digests
generated by \code{\link[=digest_partable]{digest_partable()}})
will
be used to name the models. Though
these names are not meaningful words,
the full names describing the changes
can be very long.}

\item{must_not_add_nil_parameters}{If \code{TRUE}, nil parameters (paths or
covariances fixed to zero) will not
be added in the search, implemented by
including these parameters to
\code{must_not_add}.}
}
\value{
The function \code{\link[=eq_df_models]{eq_df_models()}}
returns an \code{eq_partables} object
(a subclass of \code{partables}),
which is a list of models represented
by parameter tables.
}
\description{
Generate a list of models
with \emph{df} equal to a fitted model.
}
\details{
The following steps will be repeated
to generate models with the same
\emph{df} as a fitted model:

First, models with one more \emph{df} than
the fitted model will be generated,
by fixing more free parameter to zero.
This step is conducted by \code{\link[=drop_k]{drop_k()}}.

Second, for each of the one-more-\emph{df}
model, models with one less \emph{df} will
be generated, usually by setting one
fixed parameter to free (e.g., adding
a regression path). These models will
then have the same model \emph{df} as the
fitted model. This step is conducted
by \code{\link[=add_k]{add_k()}}.

These two steps will be repeated
until no more new models are found.
}
\examples{

library(lavaan)

# Model 1

mod1 <-
"
fm ~ fx
fy ~ fm
"
fit1 <- sem(
          model = mod1,
          data = data_test_3obvs,
          fixed.x = FALSE
        )

# Remove 'parallel = FALSE' or set parallel to TRUE
# for faster generation.
out <- eq_df_models(
  sem_out = fit1,
  parallel = FALSE
)
out

}
