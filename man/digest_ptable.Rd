% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/digest.R
\name{digest_ptable}
\alias{digest_ptable}
\alias{add_digest}
\alias{get_digest}
\title{Helpers to Compare Parameter Tables}
\usage{
digest_ptable(
  ptable,
  cols = c("lhs", "op", "rhs", "block", "group", "free", "ustart", "start"),
  digits = 6,
  sort_rows = TRUE,
  sort_by = c("lhs", "op", "rhs", "block", "group"),
  ...
)

add_digest(ptable, ...)

get_digest(ptable, ...)
}
\arguments{
\item{ptable}{A \code{lavaan} parameter
table. If it is a \code{lavaan} output,
the parameter table will be retrieved
by \code{\link[lavaan:parameterTable]{lavaan::parameterTable()}}.}

\item{cols}{The columns to be used
to compute the hash value. Note that
\code{start} and \code{ustart} will be rounded
based on \code{digits}, \code{free} will be
converted to 0s and 1s (any values
greater than 0 will be converted to
0), and \code{start} and \code{ustart} will
be set to \code{NA} for free parameters.}

\item{digits}{The number of digits
when rounding \code{ustart} and \code{start}
by \code{\link[=round]{round()}}.}

\item{sort_rows}{Whether the
parameter tables will be sorted
by \code{cols} before computing the
hash value.}

\item{sort_by}{The columns used when
sorting the rows, if \code{sort_rows} is
\code{TRUE}.}

\item{...}{For \code{\link[=digest_ptable]{digest_ptable()}},
these are optional arguments to be
passed to \code{\link[digest:digest]{digest::digest()}}. For
\code{\link[=add_digest]{add_digest()}} and \code{\link[=get_digest]{get_digest()}},
these are arguments to be passed to
\code{\link[=digest_ptable]{digest_ptable()}}.}
}
\value{
The function \code{\link[=digest_ptable]{digest_ptable()}} returns
a character string, the output of
\code{\link[digest:digest]{digest::digest()}}.

The function \code{\link[=add_digest]{add_digest()}} returns
the parameter table, with the
hash value stored in the attribute
\code{"digest"}.

The function \code{\link[=get_digest]{get_digest()}} returns
the hash value of a parameter table.
}
\description{
Helper functions that
compute and use hash digests for
parameter tables to facilitate
comparisons.
}
\details{
The function \code{\link[=digest_ptable]{digest_ptable()}} uses
\code{\link[digest:digest]{digest::digest()}} to compute a
hash value for a parameter, after
sorting some essential columns
(see the \code{cols} argument)
If two \code{lavaan} parameter tables
are identical on the values for these
columns, they should have the same
hash value will be considered as
identical.

Note that there may be cases in which
two models cannot be correctly
identified using this approach.
Nevertheless, they should be sufficient
for typical models used in this package.

The function \code{\link[=add_digest]{add_digest()}} computes
the hash value of a parameter table
and add it to the attribute \code{"digest"}
of the table,

The function \code{\link[=get_digest]{get_digest()}} retrieves
the stored hash value from a
parameter table, if available. If
not available, it will call \code{\link[=digest_ptable]{digest_ptable()}}
to compute the hash value.
}
\examples{

library(lavaan)

# Model 1

mod1 <-
"
fx =~ x1 + x2 + x3
fm =~ m1 + m2 + m3
fy =~ y1 + y2 + y3
fm ~ fx
fy ~ fm + fx
"
fit1 <- sem(
          model = mod1,
          data = data_test_3_factor_3_item,
          do.fit = FALSE
        )

mod2 <-
"
fy =~ y1 + y2 + y3
fx =~ x1 + x2 + x3
fm =~ m1 + m2 + m3
fy ~ fm + fx
fm ~ fx
"
fit2 <- sem(
          model = mod2,
          data = data_test_3_factor_3_item,
          do.fit = FALSE
        )

pt1 <- parameterTable(fit1)
pt2 <- parameterTable(fit2)

pt1
pt2

digest_ptable(pt1)
digest_ptable(pt2)

}
