% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ptables.R
\name{plot_partables}
\alias{plot_partables}
\alias{partables_plots}
\alias{plot.partables_plots}
\alias{print.partables_plots}
\alias{\%p>\%}
\title{Plot Functions for a List of Models}
\usage{
partables_plots(
  object,
  ...,
  partables = NULL,
  original_model = NULL,
  auto_node_color = TRUE,
  par_diff_settings = list(color = "blue", width = 2),
  fix_pars_fixed_zero = TRUE,
  par_fixed_zero_settings = list(color = "white", width = 0),
  exclude_original_model = FALSE,
  curve_cov = TRUE,
  curve_cov_settings = list(base_curve = 1.5)
)

\method{plot}{partables_plots}(
  x,
  ...,
  title_mode = c("digest", "name", "none"),
  title_adj = 1.4,
  title_args = list(),
  ncol = 1,
  nrow = 1,
  scale_plots = c("auto", "always"),
  scale = NULL,
  elements_to_scale = c("Nodes:width", "Nodes:height", "Edges:width", "Edges:asize"),
  original_model_mode = c("include", "exclude", "side_by_side")
)

\method{print}{partables_plots}(x, ...)

lhs \%p>\% rhs
}
\arguments{
\item{object}{If it is a list of models
(parameter tables), such as the output
of \code{\link[=eq_models]{eq_models()}}, it will be used
as the value for \code{partables}. If
it is a \code{partables_plots} object
(i.e., an output of \code{\link[=partables_plots]{partables_plots()}},
then it will be updated with any new
values for other arguments.}

\item{...}{For \code{\link[=plot.partables_plots]{plot.partables_plots()}},
these are optional arguments to be
passed to \code{\link[semPlot:semPaths]{semPlot::semPaths()}}.
For \code{\link[=print.partables_plots]{print.partables_plots()}}, they
are not used.}

\item{partables}{It should be a list of
models in the form of parameter tables,
such as the output
of \code{\link[=eq_models]{eq_models()}} or \code{\link[=eq_df_models]{eq_df_models()}}.}

\item{original_model}{The model to
which models in \code{partables} will be
compared to. If \code{NULL}, then the
plots will be generated without checking
for differences between a model and
\code{original_model}. Model comparison is
conducted by \code{\link[=model_diff]{model_diff()}}.}

\item{auto_node_color}{If \code{TRUE},
and the number of nodes is 12 or
less, they will be automatically
set to different colors. Ignored if
\code{color} of nodes is in \code{...}.}

\item{par_diff_settings}{A named list of
settings to configure the change of
a parameter from \code{original_model}
to a model in \code{partables}. For now,
two settings are supported: \code{color}
for the color and \code{width} for the
width of an edge (arrow/path).}

\item{fix_pars_fixed_zero}{If \code{TRUE},
parameters fixed to zero will be
modified based on \code{par_fixed_zero_settings}.}

\item{par_fixed_zero_settings}{A named
settings to configure edges (arrows)
fixed to zero. For now,
two settings are supported: \code{color}
for the color and \code{width} for the
width of an edge (arrow/path). Setting the
width to zero, the default, effectively
hides an arrow/path.}

\item{exclude_original_model}{If \code{TRUE}
and \code{original_model} is set, the output
will not include the plot of the original
model, though this plot will be stored
in the attribute \code{"original_model"},
as a one-element list.}

\item{curve_cov}{If \code{TRUE}, lines
denoting covariances will be automatically
curve. Implemented by calling
\code{\link[=auto_curve_covariance]{auto_curve_covariance()}}.}

\item{curve_cov_settings}{A named list
of arguments to be passed to
\code{\link[=auto_curve_covariance]{auto_curve_covariance()}}. Used only
if \code{curve_cov} is \code{TRUE}.}

\item{x}{The output of \code{\link[=partables_plots]{partables_plots()}},
a \code{partables_plots} object.}

\item{title_mode}{What will be used
as the title. If \code{"digest"}, then
the digest value of a model, generated
by \code{\link[=digest_partable]{digest_partable()}}, will be
used as its title. If \code{"name"}, then
its name in \code{x} will be used, which may
also be the digest value. If
\code{"none"}, then no title will be drawn
with the plot.}

\item{title_adj}{Adjust the position
of the title. Increase this value
if the plot is too close to the title,
and decrease this value if the space
between the plot and its title is
too large.}

\item{title_args}{A named list of
arguments to be passed to \code{\link[=title]{title()}}
when printing the title.}

\item{ncol, nrow}{The number of columns
and rows when drawing the models. Used
by \code{mfrow} in \code{\link[=par]{par()}}.}

\item{scale_plots}{How the models will
be scaled. If \code{"auto"}, then the models
will be scaled based on \code{ncol} and \code{nrow},
using \code{scale} as a reference. If
\code{"always"}, then \code{scale} will always
be used to scale the models, even if
they are drawn one by one.}

\item{scale}{How the model will
be further scaled when drawn. If this
value is greater than one, then
elements in \code{elements_to_scale} will
be increased by this ratio. If this
value is less than one, then elements
in \code{elements_to_scale} will be decreased
by this ratio.}

\item{elements_to_scale}{Elements
in the \code{qgraph} object that will be
scaled based on the values of
\code{scale_plots} and \code{scale}.}

\item{original_model_mode}{How original
model, if present, will be handled when
drawing the models. If \code{"exclude"},
then the original model will not be
drawn. If \code{"include"}, the original
model will be drawn as the first model,
along with other models. If \code{"side_by_side"},
then the number of columns is always two
and the number of rows is always one.
In each plot, the original model will
be drawn on the left and the other
model will be drawn on the right.}

\item{lhs}{A \code{partables_plots} object.}

\item{rhs}{A function call to be applied
to all stored plots. Each plot will
be inserted as the first argument.}
}
\value{
The function \code{\link[=partables_plots]{partables_plots()}} returns
a list of \code{qgraph} objects generated
from \code{\link[semPlot:semPaths]{semPlot::semPaths()}}, of the
class \code{partables_plots}.

The \code{plot} method of the output
of \code{\link[=partables_plots]{partables_plots()}} returns \code{x}
invisibly. Called for its side effect.

The \code{print} method of the output
of \code{\link[=partables_plots]{partables_plots()}} returns \code{x}
invisibly. Called for its side effect.

The \verb{\%p>\%} operator returns a
\code{partables_plots} object, processed
by the right-hand side function.
}
\description{
Various plot functions
for the output of \code{\link[=eq_models]{eq_models()}},
\code{\link[=eq_df_models]{eq_df_models()}}, and
similar functions.
}
\details{
The functions provide different ways
to visualize the models. Basic knowledge
of \code{\link[semPlot:semPaths]{semPlot::semPaths()}} from the
package \code{semPlot} is required to
customize the plots.
}
\examples{
library(lavaan)

# Model 1

mod1 <-
"
fx =~ x1 + x2 + x3
fm =~ m1 + m2 + m3
fy =~ y1 + y2 + y3
fm ~ fx
fy ~ fm + fx
"
fit1 <- sem(
          model = mod1,
          data = data_test_3_factor_3_item
        )
fit1_1_more <- drop_k(fit1)
fit1_1_more_1_less <- lapply(
  fit1_1_more,
  add_k
)

partables1 <- combine_partables(fit1_1_more_1_less)
eq_out_1 <- eq_models(
          partables1,
          original_model = fit1,
          parallel = FALSE
        )

eq_out_1 <- eq_models(
          partables1,
          original_model = fit1,
          parallel = FALSE
        )

layout_i <- matrix(c(  NA, "fm",  NA,
                     "fx",   NA, "fy"),
                   ncol = 3,
                   nrow = 2,
                   byrow = TRUE)
layout_i
p <- partables_plots(
  eq_out_1,
  original_model = fit1,
  layout = layout_i,
  label.cex = 1.5,
  sizeLat = 15,
  edge.width = 5,
  asize = 5,
  structural = TRUE,
  par_diff_settings = list(
            color = "blue",
            width = 10
          )
)
plot(p)


}
\seealso{
See \code{\link[=eq_models]{eq_models()}} on the
type of output supported.
}
