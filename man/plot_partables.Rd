% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ptables.R
\name{plot_partables}
\alias{plot_partables}
\alias{partables_plots}
\alias{plot.partables_plots}
\title{Plot Functions for a List of Parameter Tables}
\usage{
partables_plots(
  partables,
  ...,
  original_model = NULL,
  auto_node_color = TRUE,
  par_diff_settings = list(color = "blue", width = 2),
  fix_pars_fixed_zero = TRUE,
  par_fixed_zero_settings = list(color = "white", width = 0)
)

\method{plot}{partables_plots}(x, ...)
}
\arguments{
\item{partables}{It should be a list of
parameter tables, such as the output
of \code{\link[=eq_models]{eq_models()}} or \code{\link[=eq_df_models]{eq_df_models()}}.}

\item{...}{Optional arguments to be
passed to \code{\link[semPlot:semPaths]{semPlot::semPaths()}}. They
will be applied to all plots.}

\item{original_model}{The model to
which models in \code{partables} will be
compared to. If \code{NULL}, then the
plots will be generated without checking
for differences between a model and
\code{original_model}.}

\item{auto_node_color}{If \code{TRUE},
and the number of nodes is less 12 or
less, they will be automatically
set to different colors. Ignored if
\code{color} of nodes is in \code{...}.}

\item{par_diff_settings}{A named list of
settings to configure the change of
a parameter from \code{original_model}
to a model in \code{partables}. For now,
two settings are supported: \code{color}
for the color and \code{width} for the
width of an edge (arrow).}

\item{fix_pars_fixed_zero}{If \code{TRUE},
parameters fixed to zero will be
modified based on \code{par_fixed_zero_settings}.}

\item{par_fixed_zero_settings}{A named
settings to configure edges (arrows)
fixed to zero. For now,
two settings are supported: \code{color}
for the color and \code{width} for the
width of an edge (arrow). Setting the
width to zer0, the default, effectively
hide an arrow.}
}
\value{
The function \code{\link[=partables_plots]{partables_plots()}} returns
a list of \code{qgraph} objects generated
from \code{\link[semPlot:semPaths]{semPlot::semPaths()}}.
}
\description{
Various plot functions
for the output of \code{\link[=eq_models]{eq_models()}} and
similar functions.
}
\details{
The functions provide different ways
to visualize the models. Basic knowledge
of \code{\link[semPlot:semPaths]{semPlot::semPaths()}} from the
package \code{semPlot} is required.
}
\examples{
library(lavaan)

# Model 1

mod1 <-
"
fx =~ x1 + x2 + x3
fm =~ m1 + m2 + m3
fy =~ y1 + y2 + y3
fm ~ fx
fy ~ fm + fx
"
fit1 <- sem(
          model = mod1,
          data = data_test_3_factor_3_item
        )

eq_out_1 <- eq_models(
          original_model = fit1,
          parallel = FALSE
        )
# Add examples for plots

}
\seealso{
See \code{\link[=eq_models]{eq_models()}} on the
type of output supported.
}
