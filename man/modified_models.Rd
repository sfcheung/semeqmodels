% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/one_df.R
\name{modified_models}
\alias{modified_models}
\alias{get_drop_i}
\alias{get_add_i}
\title{Modified Models}
\usage{
get_drop_i(
  object,
  must_not_drop = NULL,
  progress = FALSE,
  drop_original = TRUE
)

get_add_i(
  object,
  ...,
  sem_out = NULL,
  must_not_add = NULL,
  ptable_name = NULL,
  progress = FALSE,
  remove_zeros = FALSE,
  add_name = FALSE
)
}
\arguments{
\item{object}{The original model. It
can be a \code{lavaan}-class object (the
output of \code{\link[lavaan:lavaan]{lavaan::lavaan()}} or its
wrappers, such as \code{\link[lavaan:sem]{lavaan::sem()}}).
It can also be a parameter table
generated in \code{lavaan}. If it is
a parameter table, dummy data will be
created, as it is required by
\code{\link[modelbpp:model_set]{modelbpp::gen_models()}}.}

\item{must_not_drop}{A character vector
of parameters that must not be removed,
and so will not be modified. To be
passed to \code{\link[modelbpp:model_set]{modelbpp::gen_models()}}.}

\item{progress}{Whether the model
generation process will be displayed
on screen.}

\item{drop_original}{Logical. Whether
the original model will dropped from
the output. Default is \code{TRUE}.}

\item{...}{Optional arguments to be
passed to \code{\link[modelbpp:model_set]{modelbpp::gen_models()}}.}

\item{sem_out}{A \code{lavaan} object.
Not used for now.}

\item{must_not_add}{A character
vector of parameters that must not
be added. To be passed to \code{\link[modelbpp:model_set]{modelbpp::gen_models()}}.}

\item{ptable_name}{The name of the
original model. Used only if it cannot
be generated from \code{object}.}

\item{remove_zeros}{Whether a parameter
explicitly fixed to \code{zero} will be
removed before generating modified
models.}

\item{add_name}{Whether the name of
the original model will be added as
a prefix to the names of the generated
models.}
}
\value{
The function \code{\link[=get_drop_i]{get_drop_i()}}
returns a list of the class \code{partables},
an output of \code{\link[modelbpp:model_set]{modelbpp::gen_models()}},
which are simplified versions of the
original model, usually with one or
more paths removed.

The function \code{\link[=get_add_i]{get_add_i()}}
returns a list of the class \code{partables},
an output of \code{\link[modelbpp:model_set]{modelbpp::gen_models()}},
which are more complicated versions of the
original model, usually with one or
more free parameters added.
}
\description{
Generate a list of
models a certain number of degrees
different from an original model.
}
\details{
The function \code{\link[=get_drop_i]{get_drop_i()}} is
a helper
to generate a list of models \emph{k} less
degrees of freedom different from an original
model fitted by \code{lavaan}, such as
\code{\link[lavaan:sem]{lavaan::sem()}}.

The function \code{\link[=get_add_i]{get_add_i()}} is a
helper
to generate a list of models \emph{k} more
degrees of freedom different from an
original model fitted by \code{lavaan},
such as \code{\link[lavaan:sem]{lavaan::sem()}}.
}
\examples{

library(lavaan)

mod <-
"
fx =~ x1 + x2 + x3
fm =~ m1 + m2 + m3
fy =~ y1 + y2 + y3
fm ~ fx
fy ~ fm + fx
"
fit <- sem(
          model = mod,
          data = data_test_3_factor_3_item
        )
pt <- parameterTable(fit)

# ==== Generating models one-less-df

fit_1_more1 <- get_drop_i(fit)
fit_1_more1

fit_1_more2 <- get_drop_i(pt)
fit_1_more2


# ==== Test: get_add_i ====

fit_1_less <- get_add_i(
                fit_1_more1[[1]],
                add_name = TRUE
              )

fit_1_less

}
\references{
Pesigan, I. J. A., Cheung, S. F.,
Wu, H., Chang, F., & Leung, S. O. (2026).
How plausible is my model? Assessing
model plausibility of structural
equation models using Bayesian
posterior probabilities (BPP).
\emph{Behavior Research Methods}, \emph{58}(3),
73.
\doi{10.3758/s13428-025-02921-x}
}
\seealso{
\code{\link[modelbpp:model_set]{modelbpp::gen_models()}} for
how the model generation is implemented.
}
