% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_eq_models.R
\name{gen_eq_df_models}
\alias{gen_eq_df_models}
\title{Generate Models with the Same Degrees of Freedom}
\usage{
gen_eq_df_models(
  sem_out,
  exclude_x_y_ecov = TRUE,
  parallel = FALSE,
  progress = TRUE,
  gen_models_progress = FALSE
)
}
\arguments{
\item{sem_out}{A \code{lavaan} object, which
is usually the output of \code{\link[lavaan:sem]{lavaan::sem()}}
or similar wrappers. Models will be
generated from this model.}

\item{exclude_x_y_ecov}{If \code{TRUE},
models with one or more covariance
between an exogenous variable and an
error term will be excluded.}

\item{parallel}{Whether parallel
processing will be used. (NOT READY
FOR NOW.)}

\item{progress}{If \code{TRUE}, messages
will be displayed to report the
progress of the search.}

\item{gen_models_progress}{If \code{TRUE},
then progress in each call to
\code{\link[=drop_k]{drop_k()}} or \code{\link[=add_k]{add_k()}} will also
be displayed.}
}
\value{
The function \code{\link[=gen_eq_df_models]{gen_eq_df_models()}}
returns an \code{eq_partables} objects,
which is a list of parameter tables.
}
\description{
Generate a list of models
with \emph{df} equal to a fitted model.
}
\details{
The following steps will be repeated
to generate models with the same
\emph{df} as a fitted model:

First, models with one less \emph{df} than
the fitted model will be generated,
by fixing more free parameter to zero.

Second, for each of the one-more-\emph{df}
model, models with one more \emph{df} will
be generated, usually by freeing one
fixed parameter to free (e.g., adding
a regression path). These models will
then have the same model \emph{df} as the
fitted model.

These two steps will be repeated
until no more new models are found.
}
\examples{

# TODO:
# The example is too long to run. Shorten it.

library(lavaan)

# Model 1

mod1 <-
"
fx =~ x1 + x2 + x3
fm =~ m1 + m2 + m3
fy =~ y1 + y2 + y3
fm ~ fx
fy ~ fm + fx
"
fit1 <- sem(
          model = mod1,
          data = data_test_3_factor_3_item
        )

out <- gen_eq_df_models(
  sem_out = fit1
)
out

}
